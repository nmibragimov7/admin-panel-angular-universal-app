import { NgModule } from "@angular/core";

import { DateFilter } from "../core/pipe/date.pipe";

@NgModule({
  imports: [],
  providers: [],
  declarations: [DateFilter],
  exports: [DateFilter]
})

export class SharedModule { }
